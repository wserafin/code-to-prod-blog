apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: reversewords
  namespace: reverse-words-stage
spec:
  path: /
  to:
    kind: Service
    name: reverse-words
  port:
    targetPort: 8080
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect  
